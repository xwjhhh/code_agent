import { AlertTriangle, Check, Clock3, ScanSearch } from "lucide-react";

export function ReviewerCard({ content, verified = false, loading = false }: { content?: string; verified?: boolean; loading?: boolean }) {
  return <section className="reviewer-card"><div className="panel-header"><div style={{ display: "flex", alignItems: "center", gap: 7 }}><ScanSearch style={{ width: 14, color: "var(--purple)" }} /><span className="panel-title">代码评审</span></div><span className="badge purple">独立评审</span></div><div className="reviewer-score"><div><strong style={{ fontSize: 15, letterSpacing: 0 }}>{loading ? "评审中" : content ? "已完成" : "未开始"}</strong></div><span className={`badge ${verified ? "green" : "yellow"}`}>{verified ? <Check /> : <Clock3 />} {verified ? "本地测试通过" : "等待本地验证"}</span></div><div className="reviewer-notes">{content ? <div className="review-note" style={{ whiteSpace: "pre-wrap", lineHeight: 1.8 }}><Check /> <span>{content}</span></div> : <div className="review-note warn"><AlertTriangle /> <span>只有本地测试通过后才会启动 Reviewer。评审结论不会声称保证通过线上隐藏测试。</span></div>}</div></section>;
}
