# Code Agent 项目面试提问清单与答题要点

> 适用项目：`my_agent_code`（Python 算法题编程智能体）  
> 资料依据：项目 README、`doc/` 架构文档、`src/code_agent/` 实现和 `tests/` 测试。  
> 使用方式：先背“项目事实”和 1 分钟介绍，再按面试官的追问方向展开。每道题至少准备“结论 + 代码位置 + 一个取舍/风险”。

## 0. 先把项目讲准确

### 一句话介绍

这是一个面向 Python 算法题的本地编程智能体。用户提供题目和输入输出用例后，模型通过唯一的 Bash 工具在独立工作目录里编写 `solution.py`，由固定的 pytest 测试执行真实反馈，失败就继续修改，测试通过后再由只读 Reviewer 独立评审，并把经过验证的策略/修复经验写入长期记忆。

### 关键事实速记

| 维度 | 项目事实 | 对应位置 |
|---|---|---|
| Agent 主循环 | `Messages -> Model -> Bash Action -> LocalEnvironment -> Observation -> Messages` | [`agents/default.py`](src/code_agent/agents/default.py) |
| 模型 | 固定 `openai/deepseek-ai/DeepSeek-V4-Flash`，经 LiteLLM 调用硅基流动 OpenAI 兼容接口 | [`models/litellm_model.py`](src/code_agent/models/litellm_model.py) |
| 工具 | 只有 Bash；模型输出 tool call，系统解析后执行 | [`models/utils/actions.py`](src/code_agent/models/utils/actions.py) |
| 测试契约 | `test_cases.json` 是题目输入输出的权威文件；`test_solution.py` 由系统生成且受保护 | [`test_cases.py`](src/code_agent/test_cases.py)、[`environments/local.py`](src/code_agent/environments/local.py) |
| 完成条件 | 三个文件存在、精确执行 `python -m pytest -q`、有真实通过输出、提交命令合法 | [`agents/default.py`](src/code_agent/agents/default.py) |
| Reviewer | 仅在本地验证成功后运行，只读分析算法、复杂度、边界和测试覆盖 | [`reviewer.py`](src/code_agent/reviewer.py) |
| 记忆 | Qwen3-Embedding-8B + SQLite；任务检索、失败恢复检索、成功/失败经验学习和去重 | [`memory/manager.py`](src/code_agent/memory/manager.py) |
| 服务 | FastAPI 后台线程运行，SSE 推送事件；Next.js 展示运行、文件、测试、轨迹、评审和记忆图 | [`api.py`](src/code_agent/api.py)、`frontend/` |
| 产物 | `workspace/<run_id>/`、`trajectories/<run_id>/`、`memory_store/memory.sqlite3` | [`doc/ARCHITECTURE.md`](doc/ARCHITECTURE.md) |

### 必须主动说明的边界

- 本地测试通过只代表通过当前用例，不等于通过在线评测的隐藏用例。
- `LocalEnvironment` 使用 Git Bash 和当前进程权限执行命令；“独立工作目录”不是完整的操作系统沙箱。生产环境应增加容器、非 root 用户、资源限制、网络隔离和系统调用限制。
- 记忆是辅助上下文，题目和权威测试优先；记忆服务不可用时应降级，不应阻塞主编程闭环。
- 仓库中有展示页和历史快照，展示页不连接 FastAPI，也不执行任务；不要把它描述成线上推理服务。
- 面试时只使用自己实际跑过或能展示证据的数字，不要直接背模板中的 QPS、准确率等未经测量的指标。

## 1. 一分钟项目介绍

> 我做的是一个 Python 算法题编程智能体。用户提交题目和多组标准输入输出后，系统先统一生成 `test_cases.json` 和固定的 `test_solution.py`，再把题目交给 DeepSeek。模型只能通过 Bash 读文件、写 `solution.py` 和执行测试，系统把真实命令结果作为 tool observation 回传，形成“生成—执行—反馈—修复”的循环。只有精确执行 `python -m pytest -q` 并且真实通过、三个文件都存在时，才允许提交；随后由独立只读 Reviewer 分析算法和复杂度。每次运行会保存轨迹、评审和工作区文件；验证成功的运行还会提炼成结构化经验，用 Embedding + SQLite 检索到后续相似题目中。Web 端通过 FastAPI、后台线程和 SSE 展示实时过程。项目的核心价值不是一次生成代码，而是把可验证的执行闭环、失败恢复和经验复用做成了工程系统。

## 2. 架构与流程题（优先级：最高）

### Q1：请画出系统的完整数据流。

**答题要点：**

1. 用户提交题目和手工用例，或调用模型生成用例。
2. 规范化后写入工作区 `test_cases.json`，同时生成固定的 `test_solution.py`。
3. 可选地从 Memory 做任务路由、查询改写、向量召回、LLM 重排和相关性判断。
4. `DefaultAgent` 发送 system/task 消息给模型；模型返回 Bash tool call。
5. `LocalEnvironment` 在该 run 的目录执行命令，返回 stdout/stderr、return code、异常信息。
6. observation 带原始 `tool_call_id` 回传模型；pytest 失败时追加 Recovery 记忆上下文。
7. 测试通过后允许精确提交，调用 Reviewer，最后持久化轨迹、评审并学习记忆。
8. API 层把事件放入 `RunState.events`，SSE 推给前端。

### Q2：为什么把 Agent 循环自己实现，而不是直接使用 LangChain/LangGraph？

**答题要点：**

- 任务只有一个明确的编程循环，自己实现能精确控制工具格式、测试门禁、文件保护和结束条件。
- 协议层通过 `Model`、`Environment`、`Agent` Protocol 解耦，替换模型或执行环境不需要改循环。
- 没有引入大型编排框架，依赖更少、轨迹更透明，便于测试每个状态转移。
- 代价是需要自行处理重试、持久化、并发、恢复和可观测性；任务复杂到多节点并行或人工审批时，可以考虑 LangGraph 的 StateGraph/checkpoint。

### Q3：这个 Agent 属于哪种模式？和 ReAct 有什么关系？

**答题要点：**

- 它是工具调用驱动的 ReAct 风格循环：模型根据当前消息决定下一步行动，环境执行后返回 observation，再决定下一步。
- 与经典“显式输出 Thought/Action/Observation”不同，内部主要保存 assistant tool call 和 tool observation，不要求把思维链暴露给用户。
- Agent 的有效状态包括消息历史、步骤、`verified`、调用次数、测试输出和事件历史。
- 完成判断由代码不变量控制，不能只相信模型说“完成了”。

### Q4：一次 `DefaultAgent.run()` 具体如何结束？

**答题要点：**

- `_reset()` 初始化 system/task 消息和可选记忆。
- 循环调用 `query()` 和 `execute_actions()`，每次模型调用计入 `n_calls`。
- 测试命令通过后设置 `verified=True`；如果之后执行了非提交命令，立即把它置回 `False`。
- 只有真实执行精确的 `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT` 且 `verified=True`，才将状态设为 `success`。
- 达到 `step_limit` 进入 `max_steps`；模型调用失败且尚未验证时进入 `model_error`；所有路径都会记录 `agent_finished` 并保存轨迹。

### Q5：为什么不能让模型自己判断测试是否通过？

**答题要点：**

- 模型文本可能误读 stdout，甚至声称“已通过”但没有执行命令。
- 代码检查 `returncode == 0`、匹配 `1 passed` 以上的实际 pytest 输出、三个文件存在，形成可验证门禁。
- 提交命令只是模型表达“我准备结束”，最终是否成功仍由 Agent 的状态决定。
- 仍有局限：测试集不完整、输出匹配规则是启发式，因此不能替代隐藏测试和人工审查。

### Q6：为什么测试通过后还要求“立即提交”？

**答题要点：**

- 防止模型在已经验证的代码上继续修改，导致“测试通过的版本”和“最终版本”不一致。
- `execute_actions()` 检测验证后的非提交命令并撤销 `verified`，迫使模型重新测试。
- 提交命令使用固定 sentinel，解析简单、可审计，也避免把自然语言当成完成信号。

### Q7：模型一次返回多个 Bash tool call 怎么处理？

**答题要点：**

- `parse_tool_calls()` 逐个校验工具名、参数、非空 command 和 `tool_call_id`。
- Agent 按顺序执行每个 action，分别记录开始/结束事件和 observation，再按相同顺序生成 tool 消息。
- 如果任意 action 修改受保护文件，环境恢复文件并返回失败信息；后续 action 仍需明确记录，便于追踪。
- 需要继续关注多 action 的原子性：当前是顺序执行，不是事务；生产环境可限制每次一个 action 或提供更细粒度权限。

### Q8：模型返回非法 tool call 时怎么办？

**答题要点：**

- 缺少 id、未知工具、参数不是合法 JSON、command 为空都会抛 `FormatError`。
- Agent 把格式错误作为消息追加回模型，要求重新返回合法 Bash call，而不是直接崩溃。
- 若连续格式错误耗尽调用次数，最终受 `step_limit` 控制；应在监控中统计格式错误率。

### Q9：如何防止 Agent 无限循环？

**答题要点：**

- `step_limit` 限制模型调用次数；API 还限制 `max_steps` 在 1 到 100。
- Bash 每条命令有 timeout；模型调用也配置 timeout 和最大重试次数。
- 提交前必须通过测试；未知路由、异常和错误状态都有明确退出路径。
- 可继续增加“相同命令/相同失败输出重复次数”熔断、总预算和死循环检测。

### Q10：模型在测试通过后突然网络失败，项目如何处理？

**答题要点：**

- `DefaultAgent` 已经拥有 `verified=True` 时，把后续 `ModelError` 视为可恢复终态，标记 `recovered_from_model_error=True` 并成功结束。
- 原因是本地验证已经形成事实证据，后一个请求通常只是请求提交 sentinel；不能因为供应商短暂故障把已验证结果判成失败。
- 若模型在验证前失败，则进入 `model_error`，不能伪造成功。
- 这是一个取舍：提高可用性，但仍应在轨迹中显式标记“未收到提交 sentinel”。

## 3. 测试用例与正确性题（优先级：最高）

### Q11：为什么设计 `test_cases.json` 作为唯一权威输入？

**答题要点：**

- 人工用例和模型生成用例最终统一成同一种 JSON 结构，降低链路分支。
- 编程模型、固定 pytest runner 和 Reviewer 读取同一份数据，避免三者对输入输出格式理解不一致。
- 文件在 Agent 启动前生成，执行过程中受保护，模型不能修改期望答案来“修复”失败。
- `normalize_cases()` 把空输入/空输出也转成字符串，保证数据契约稳定。

### Q12：AI 生成测试用例有什么风险？

**答题要点：**

- 模型可能生成不符合题意、答案错误或覆盖不足的用例，所以前端允许用户修改，不能把生成结果当作真值证明。
- `_parse_json_array()` 只负责格式解析，不能验证算法答案的语义正确性。
- 更强方案是用参考实现、差分测试、约束求解器、随机/性质测试校验期望输出，并保留人工样例。

### Q13：固定 `test_solution.py` 如何工作？

**答题要点：**

- 它读取 `test_cases.json`，导入 `solution.solve`，对每个 case 调用 `solve(input_text)`。
- `normalize_output()` 统一 `\r\n` 和 `\n`，并去掉末尾换行，减少跨平台格式噪声。
- 使用 pytest 参数化，每个 case 有独立名称，失败报告可定位。
- 强制题解提供 `solve(input_text: str) -> str`，将交互式 stdin/stdout 转成可测试函数契约。

### Q14：`verified` 的判定条件是什么？为什么要检查多个条件？

**答题要点：**

- 测试命令必须精确等于 `python -m pytest -q`。
- 返回码必须为 0，输出必须包含至少一个非零 passed 数量，且 `solution.py`、`test_cases.json`、`test_solution.py` 都存在。
- 多条件避免“运行了别的命令”“空测试集”“文件缺失”等假阳性。
- 这仍是本地验证，不覆盖性能、隐藏数据、资源限制和题目未表达的语义。

### Q15：如何防止模型篡改测试文件？

**答题要点：**

- `LocalEnvironment` 初始化时保存受保护文件的 bytes 快照。
- 每条命令结束后比较当前内容，发现变化就恢复原内容、将 return code 改成 `-1`，并返回明确的安全错误。
- Agent 还在模型提示词中明确禁止修改两个文件。
- 当前保护是“命令后检测并恢复”，不是强隔离；更强实现应使用只读挂载、权限隔离或容器文件系统。

### Q16：为什么要记录 `solution_before/after`，不能只看命令字符串吗？

**答题要点：**

- 模型可以通过 heredoc、脚本或 `python -c` 修改文件，命令文本未必包含 `solution.py`。
- Agent 在每个命令前后读取 `solution.py`，变化时发送 `file_changed` 事件并保存快照。
- RecoveryEpisodeBuilder 使用这些证据识别“失败测试 -> 修改 -> 通过测试”的真实修复片段。

### Q17：为什么测试通过后仍需要 Reviewer？

**答题要点：**

- 当前测试只能证明给定样例通过，Reviewer 进一步检查算法思想、复杂度、边界和测试覆盖。
- Reviewer 只在 `status=success && verified=True` 时运行，且不执行命令、不修改文件，减少评审污染。
- Reviewer 明确要求不能声称隐藏测试一定通过。
- 它是辅助判断，不是形式化验证；关键业务仍需要人工或参考实现。

### Q18：如何评价这个系统的“正确率”？

**答题要点：**

- Agent 层：本地验证成功率、平均模型调用次数、失败后恢复率、提交前违规率。
- 测试层：按题型统计隐藏测试通过率、边界用例覆盖率、测试生成有效率。
- 记忆层：Recall@K、经验被采纳后的通过率变化、错误记忆触发率、重复记忆率。
- 服务层：P50/P95 延迟、模型错误率、超时率、SSE 断线重连成功率和成本。
- 必须建立固定离线题集和回归基线，按版本做 A/B，不只看单次成功。

## 4. 本地执行、权限与安全题（优先级：最高）

### Q19：`LocalEnvironment` 如何执行命令？

**答题要点：**

- Windows 优先解析 Git for Windows 的 `bash.exe`，通过 `[bash_path, "-lc", command]` 执行。
- 设置 run 工作目录 `cwd`，合并进程环境变量和配置环境变量，捕获 stdout/stderr，统一 UTF-8 解码。
- `subprocess.run(..., timeout=...)` 控制单命令最长时间，超时返回 `returncode=-1` 和 `exception_info`。
- 所有结果统一为 `output`、`returncode`、`exception_info`，便于模型和前端消费。

### Q20：这个执行环境安全吗？面试官如果质疑任意 Bash 怎么回答？

**诚实回答：**

- 当前版本解决了工作目录隔离、测试文件保护和超时，但仍然允许命令以服务进程权限运行，不能称为生产级沙箱。
- 风险包括读写宿主机文件、网络访问、进程创建、资源耗尽、命令注入和窃取环境变量。
- 生产改进：每个 run 使用短生命周期容器/微虚拟机；非 root 用户；只读基础镜像；限制 CPU、内存、磁盘、进程和网络；清理环境变量；挂载独立工作目录；seccomp/AppArmor/Windows Job Object；命令白名单或结构化工具。
- 安全策略应分层：输入过滤、工具权限、执行沙箱、输出脱敏和审计告警。

### Q21：如何防止命令超时和资源耗尽？

**答题要点：**

- 当前有单命令 timeout 和 Agent `step_limit`，模型请求也有 timeout/retry。
- 生产还要加 CPU/内存/磁盘/进程数限制，限制输出长度，杀死进程树而不只是父进程，设置总 run deadline。
- 对 pytest 结果做截断和结构化传递，避免超大日志占满上下文。
- 对重复失败、重复命令和异常退出增加熔断与人工接管。

### Q22：为什么保存 bytes 而不是文本快照？

**答题要点：**

- bytes 能无损恢复原文件，不受编码、换行和非法字符影响。
- 运行输出则用 UTF-8 + `errors="replace"`，优先保证 Agent 不因日志编码异常崩溃。

### Q23：执行命令失败后，哪些错误应该重试？

**答题要点：**

- 模型供应商的暂时网络错误、限流和 5xx 可以指数退避重试。
- Bash 超时、语法错误、测试断言失败通常需要模型根据新 observation 修改，不应盲目重复同一命令。
- 文件保护违规、权限错误和未知工具格式错误应快速失败或要求重新规划。
- 重试必须有上限、幂等性和可观测记录，避免费用和副作用放大。

## 5. 模型适配与 Tool Use 题（优先级：高）

### Q24：为什么使用 LiteLLM？

**答题要点：**

- 让模型调用统一为 `completion()`，屏蔽不同供应商的 OpenAI 兼容细节。
- 项目自身仍负责 Agent 循环、Bash 执行、测试门禁和完成判断；LiteLLM 不是 Agent 框架。
- 通过 `Model` Protocol 保留替换为 FakeModel 的测试能力。
- 当前用 `validate_model_name()` 固定 DeepSeek V4 Flash，保证演示配置一致；通用产品可改为白名单模型注册表。

### Q25：`query()` 和 `query_text()` 为什么分开？

**答题要点：**

- `query()` 发送 Bash tool schema，返回 assistant 消息和解析后的 actions，供主 Agent 循环使用。
- `query_text()` 不带工具，用于测试生成、Reviewer、Memory 路由/抽取/重排等文本任务。
- 分离可以避免 Reviewer 或记忆模块意外执行命令，也让提示词和错误处理更简单。

### Q26：tool call 到 observation 的关联为什么必须保留 `tool_call_id`？

**答题要点：**

- 一个 assistant 消息可能有多个工具调用，模型需要靠 id 将每个结果对应回原 action。
- 丢失 id 会造成消息协议不合法或结果串线。
- `format_observation_messages()` 为每个 action 生成 role=tool、同 id 的 JSON 内容，并保留原始 action/observation 供轨迹记录。

### Q27：如何处理不同 SDK 返回的消息对象？

**答题要点：**

- `_get()` 同时支持 dict 和对象属性。
- `_format_response()` 优先 `model_dump()`，其次 dict，再退回 `vars()`，最后统一写入 `extra.actions` 和原始 response。
- 这样可以适配 LiteLLM/Pydantic 版本差异，但应配合契约测试锁定真正支持的返回形状。

### Q28：重试为什么使用指数退避？有什么隐患？

**答题要点：**

- 第 `attempt` 次等待 `2**attempt` 秒，给限流或暂时网络故障恢复时间，避免立即打爆供应商。
- 当前有最大重试次数，但没有抖动；多实例同时重试可能形成惊群，生产应加随机 jitter、按错误类型分类和总 deadline。
- 对非幂等工具调用不能简单重试；当前模型调用本身无外部副作用，Bash 执行不在 LiteLLM 重试范围内。

### Q29：怎样保证模型输出的结构化格式？

**答题要点：**

- 提示词要求只返回 JSON；代码使用 `json.loads` 和类型/字段校验。
- 测试生成允许剥离 ```json 围栏；Memory 各模块使用安全 JSON 解析，失败走 fallback。
- 对关键路由可使用 provider 的 JSON schema/structured output，再加 Pydantic 校验；不能仅依赖 prompt。

### Q30：如何处理 API Key 和敏感信息？

**答题要点：**

- Key 从环境变量读取，`.env` 不入库，不放前端。
- `LitellmModel.serialize()` 对 key/token/secret/password 字段脱敏；展示导出排除密钥、向量、模型消息和工作区路径。
- 生产应使用 Secret Manager、最小权限、密钥轮换和日志脱敏，并避免把题目中的敏感数据发送到第三方模型。

## 6. Agent 记忆系统题（优先级：最高，项目差异化亮点）

### Q31：为什么要给算法编程 Agent 加长期记忆？

**答题要点：**

- 同一用户会反复遇到滑动窗口、DP、图遍历、输入解析等模式；只靠当前上下文无法跨运行复用。
- 记忆保存可迁移的策略、失败原因和修复动作，而不是完整旧对话，降低上下文噪声和 token 成本。
- 记忆是建议，不是答案；当前题目、约束和权威测试始终优先。

### Q32：MemoryNode 保存哪些信息？

**答题要点：**

- 顶层 `experience_type`：`success` 或 `failure`。
- `category`：`strategy`、`recovery`、`optimization`。
- 触发条件 `trigger`、可迁移内容 `content`、行动步骤 `steps`、反例 `negative_example`。
- 题型/算法标签、约束、优先级、质量分、来源 run、验证证据、失败/修复/验证字段。
- `embedding_text` 用于向量化；向量和 source-task 向量只在后端存储，不直接展示给用户。

### Q33：任务开始时记忆检索的完整流程是什么？

**答题要点：**

1. 空库直接 skip，不调用模型和 Embedding。
2. `MemoryRouter` 判断 `retrieve` 或 `skip`，并生成短查询；路由失败回退到原任务检索。
3. `QueryAnalyzer` 将题目改写为多个语义查询并抽取题型/算法标签。
4. Embedding 客户端批量向量化，SQLite 做候选搜索和元数据过滤。
5. 以 `0.6 * task_similarity + 0.4 * memory_similarity` 排序，按相似度、质量和优先级取候选。
6. 候选过多时由 LLM Reranker 选最多 4 条非重复经验。
7. `MemoryRelevanceGrader` 判断是否真正可执行；不相关则最多改写两次查询。
8. 通过 `format_memory_context()` 注入 Agent 的 user 消息，并发出事件。

### Q34：任务记忆和 Recovery 记忆有什么区别？

**答题要点：**

- 任务记忆面向“开始解题”：召回算法策略、题型经验和优化建议。
- Recovery 记忆面向“pytest 刚失败”：查询中加入题目、失败输出和最近动作，只过滤 `experience_type="failure"` 的经验。
- Recovery 上下文仅在测试失败后注入，避免一开始污染上下文。
- 两者都允许降级，但不能把失败经验当成成功策略直接使用。

### Q35：为什么不能看到一次失败就写入失败记忆？

**答题要点：**

- 单次失败无法证明某个修复动作有效，可能只是 flaky、误判或尚未完成。
- `RecoveryEpisodeBuilder` 要求连续证据：失败测试 -> 发生 `solution.py` 修改 -> 同一流程后续测试通过。
- 多次失败会合并为一个 episode，避免一个 bug 膨胀出多条记忆。
- 最终失败或只有 Reviewer 风险提示，不足以生成 evidence-backed failure memory。

### Q36：成功经验什么时候允许写入？

**答题要点：**

- run 必须 `verified=True`。
- Reviewer 不能明确判定算法错误或语义不满足题意；否则拒绝成功记忆。
- Extractor 还要产生合法的结构化字段，之后才 Embedding、去重、入库。
- 这是一种“验证门禁 + 评审门禁”，降低错误经验污染。

### Q37：向量搜索是如何计算的？

**答题要点：**

- Embedding 客户端调用硅基流动 `/v1/embeddings`，默认模型 `Qwen/Qwen3-Embedding-8B`。
- `MemoryStore` 将向量 JSON 序列化在 SQLite 中，本地计算余弦相似度。
- 如果有 source-task 向量，综合任务相似度和记忆正文相似度；没有时兼容旧数据只用记忆向量。
- 当前实现是读取候选后逐条计算，适合本地小规模库；规模变大应迁移 pgvector/Milvus/专用 ANN 索引。

### Q38：为什么还要用 LLM Reranker 和相关性 Grader？向量相似度不够吗？

**答题要点：**

- 向量相似度容易把“关键词相似但算法约束相反”的经验召回。
- Reranker 关注可执行性、非重复性和质量；Grader 判断至少一条经验是否真的适用于当前问题。
- 相关性不足时有限次查询改写，避免无限重试。
- LLM 也会误判，所以候选先由向量缩小，所有结果都有 fallback；可以加入离线标注集评估和规则校验。

### Q39：记忆如何去重？

**答题要点：**

- 先按同一 `experience_type` 做向量候选召回，避免 success 和 failure 互相去重。
- 相似度达到候选阈值后，由 LLM 比较适用条件、因果关系和行动建议。
- LLM 不可用时回退到高阈值 `duplicate_threshold` 的向量判断。
- 不同算法、相反遍历方向或重要约束不同，不能只因为词面相似就合并。

### Q40：`source_verified` 和旧数据迁移解决什么问题？

**答题要点：**

- `source_verified` 标记经验是否来自可验证运行；默认检索只看 verified 数据。
- SQLite 启动时补齐新列、迁移旧 schema，并把没有来源任务/证据的旧 recovery 数据隔离为未验证。
- 这样 schema 演进不会直接把历史推测当成可靠知识。
- 生产需要正式 migration 版本、备份和回滚，而不是只依赖启动时 ALTER TABLE。

### Q41：Embedding 或 Memory 服务挂了，主流程会怎样？

**答题要点：**

- Memory 是 advisory；初始化、路由、Embedding、重排或学习异常会被捕获，主 Agent 仍可运行。
- 事件中记录 `memory_error`，运行状态里保存错误原因，方便排查。
- 任务检索失败不应改变题目或测试文件；学习失败只影响后续经验，不影响已完成结果。
- 可进一步加缓存、熔断、异步学习队列和本地规则检索回退。

### Q42：记忆会不会把错误建议注入模型？

**答题要点：**

- 通过验证成功、Recovery episode、Reviewer gate、`source_verified`、相关性判断和去重降低风险。
- 注入内容应明确标识为 advisory，并要求模型根据当前题目和测试验证，不能照抄。
- 经验应限制条数、长度和优先级；对高风险冲突经验做拒答或人工审核。
- 最终 correctness gate 仍是独立测试，不是记忆内容。

## 7. API、SSE 与前端题（优先级：高）

### Q43：为什么 `POST /api/runs` 使用后台线程？

**答题要点：**

- 创建请求只负责生成 run id、初始化 `RunState`、启动线程并立即返回 202，避免浏览器请求被模型和 pytest 阻塞。
- Agent 的每个阶段通过 `state.emit()` 写入事件；前端再订阅事件流。
- 当前是单进程内线程模型，适合本地演示；生产应使用任务队列/Worker，状态放 Redis/数据库，并限制并发和隔离资源。

### Q44：SSE 事件如何保证顺序和断线重连？

**答题要点：**

- `RunState.emit()` 在条件锁内按 `len(events)` 生成递增 `sequence`，同时记录 UTC timestamp。
- SSE 输出 `id`、`event` 和 JSON `data`；客户端可以传 `Last-Event-ID`，服务端从下一个位置继续。
- 前端按 sequence 去重，收到 `run_finished` 后关闭连接。
- SSE 是服务端到客户端的单向流；若需要双向交互或人工中断，可考虑 WebSocket/独立控制 API。

### Q45：为什么使用 `threading.Condition`？

**答题要点：**

- 事件生成器在没有新事件时等待，避免忙轮询；`emit()` 添加事件后 `notify_all()` 唤醒订阅者。
- 对 `events` 的读取和追加需要一致性，锁也防止多个线程同时创建/更新状态时竞态。
- 进程重启后通过 `trajectory.json` 恢复已完成运行；正在运行的任务仍无法靠当前内存恢复，应由 Worker checkpoint 补足。

### Q46：API 如何校验输入？

**答题要点：**

- Pydantic `RunRequest` 校验题目非空、模型白名单、`max_steps`、timeout、测试来源和开关。
- `TestCaseInput` 统一字段；无手工用例时调用模型生成。
- FastAPI 对非法模型返回 422，模型/供应商失败转换为 502，运行不存在返回 404。
- 生产还要做认证、租户隔离、请求限流、幂等键、题目长度限制和审计。

### Q47：前端展示的“历史运行”从哪里来？

**答题要点：**

- 后端启动时扫描 `trajectories/*/trajectory.json`，恢复完成运行的元数据、结果、事件和评审。
- 当前 `RUNS` 是进程内字典，持久化文件主要用于重启后显示历史；不是高并发数据库。
- 展示页 `docs/index.html` 使用导出后的 JSON 静态数据，不会调用模型或 FastAPI。

### Q48：Memory Graph 的边如何生成？

**答题要点：**

- 同一个 `source_run_id` 的记忆之间建立实线分组关系。
- 记忆之间计算余弦相似度，超过阈值后建立虚线相似关系。
- 每个节点最多参与 3 条相似边，避免图过密；向量不返回浏览器。
- 这是展示用稀疏图，不应误称为完整知识图谱或推理图。

## 8. 工程质量与测试题（优先级：高）

### Q49：项目如何做单元测试？

**答题要点：**

- 模型、环境和 Embedding 都可用 Fake/Mock 替代，测试不依赖真实 API Key。
- Agent 测试提交门禁、失败状态、模型错误恢复、Recovery 注入和隐藏编辑快照。
- Environment 测试命令参数、cwd、超时和受保护文件恢复。
- Memory 测试持久化、元数据过滤、schema 迁移、去重、加权搜索、路由 skip、query rewrite 和 evidence episode。
- API 测试健康检查、模型校验、测试生成和 SSE sequence/重连。

### Q50：如何测试异步/并发的 SSE？

**答题要点：**

- 先对 `RunState.emit()` 测顺序号和事件结构，再对事件生成器测 cursor/Last-Event-ID。
- 用确定性的 Fake Agent 注入事件，避免测试等待真实模型。
- 增加并发压测：多个 run 同时创建、一个 run 多个订阅者、断线重连、客户端慢消费。
- 检查锁粒度、内存增长和任务结束后的资源清理。

### Q51：当前测试覆盖有哪些不足？

**答题要点：**

- 没有真实供应商的稳定集成测试，工具调用返回格式变化可能漏测。
- 没有完整的 OS 沙箱安全测试、恶意命令测试、进程树清理和资源限制测试。
- 没有真实隐藏评测集，无法证明算法泛化率。
- API 当前状态管理偏单进程，缺少多 Worker、重复请求和故障恢复测试。
- 记忆评估还应有标注相关性集、错误注入集和 embedding 模型升级回归。

### Q52：如何定位一次运行失败？

**答题要点：**

1. 先看 `run_finished`、状态和 `verified`。
2. 查看 `trajectory.json` 的 model message、action、observation 和每步快照。
3. 区分模型错误、tool 格式错误、Bash 超时、测试断言失败、测试文件保护违规和 Reviewer/Memory 错误。
4. 对 pytest 失败比对输入、期望输出、实际输出和最近一次 `solution.py` 变化。
5. 查看 `memory_retrieval`、`memory_context_injected` 和 `memory_learning` 事件，确认记忆是否干扰或缺失。

### Q53：为什么轨迹要落盘？

**答题要点：**

- 支持复盘、调试、离线评估、历史页面和经验抽取。
- 保存任务、模型/环境元数据、消息、动作、观察、测试结果、事件和最终状态。
- 轨迹可能含题目、代码和模型输出，生产需要脱敏、访问控制、保留期限和加密。
- `RunStorage` 负责 JSON 原子边界之外的基础持久化；更强实现应使用临时文件 + rename，防止进程中断留下半文件。

## 9. 通用 AI Agent / RAG 八股（项目会被横向追问）

### Q54：Function Calling 和 ReAct 是什么关系？

**答题要点：**

- ReAct 是“根据观察规划下一步”的控制思想；Function Calling 是模型输出结构化工具参数的协议。
- 二者互补：本项目用 tool call 表达 Bash Action，用 observation 驱动下一轮决策。
- 工具调用不等于安全授权，仍需代码校验、权限、沙箱、超时和审计。

### Q55：Agent、Workflow 和普通 Chatbot 的区别？

**答题要点：**

- Chatbot 主要生成回复；Workflow 的步骤和分支由代码预先决定；Agent 可以根据环境反馈动态选择下一动作。
- 本项目测试失败后是否继续、如何修改，是模型根据真实 observation 决定，但测试门禁和安全边界由代码固定。
- 可控系统通常是“代码约束的 Agent”，不是完全自由的自治程序。

### Q56：什么是 Prompt Injection？本项目如何防？

**答题要点：**

- 不可信题目、文件或命令输出可能诱导模型泄露秘密、修改测试或执行危险操作。
- 当前措施：唯一 Bash 工具、系统提示词规定文件和命令契约、受保护文件恢复、提交门禁、环境超时、模型/工具 observation 结构化。
- 这些不能替代沙箱；生产还要隔离 secrets、过滤外部内容、工具最小权限、输出检查和人工审批。

### Q57：RAG 和微调怎么选？

**答题要点：**

- RAG 适合动态知识、需要引用和快速更新；微调适合稳定行为、格式、风格或领域能力。
- 本项目的历史经验是动态增长且需要可追溯来源，所以采用 Embedding 检索，不把每次经验写进模型参数。
- 经验内容仍需要结构化、去重、验证和上下文预算控制。

### Q58：Embedding、BM25、Rerank 分别解决什么问题？

**答题要点：**

- Embedding 用语义向量找表达不同但含义相近的内容。
- BM25/关键词检索擅长精确匹配标识符、算法名、错误信息和数字约束。
- Rerank 对少量候选做更精细的 query-document 判断，通常比直接扩大向量 top-k 更有效但更慢更贵。
- 本项目的 Memory 当前是向量 + metadata + LLM 重排；如扩展到题目文档库，可采用混合召回和 RRF。

### Q59：如何解决大模型幻觉？

**答题要点：**

- 给模型真实环境反馈，而不是只让它自我判断。
- 使用固定测试、独立 Reviewer、结构化输出和工具结果。
- 让模型明确不确定性，失败时要求读取具体输出再修复。
- 用离线评测、引用/证据、规则校验和人工抽检监控，不把“模型说对了”当作证据。

### Q60：如何控制上下文长度和成本？

**答题要点：**

- 只注入最多少量高相关 Memory；截断任务、失败输出、最近动作和代码快照。
- 任务检索与 Recovery 检索分开，避免重复塞入全部历史。
- 限制最大步骤和模型重试；对测试生成、路由、重排和 Reviewer 使用合适的小模型或批处理。
- 记录 input/output tokens、每 run 成本和失败重试次数，支持预算熔断。

### Q61：如何让输出稳定遵循 JSON？

**答题要点：**

- 使用 JSON schema/structured output、低 temperature、清晰字段定义和示例。
- 解析后做类型、枚举、长度和语义校验；失败时有限次重试并保留原始响应。
- Fallback 必须有明确默认行为，不能默默接受半截或不可信数据。

### Q62：怎样设计高并发版本？

**答题要点：**

- API Gateway -> 任务队列 -> Agent Worker；每个任务独立容器/沙箱。
- Redis/数据库保存 RunState 和事件，SSE 网关从持久化事件读取；对象存储保存代码/轨迹。
- 模型调用限流、批处理和缓存；Embedding 使用批量请求；SQLite 迁移到支持并发的存储。
- 任务幂等键、租约、心跳、超时回收、重试队列和租户配额必须明确。

## 10. 结合本项目的系统设计追问

### Q63：如果题目规模增加到每天十万道，最先改什么？

**答题要点：**

- 先拆 API 与 Worker，避免 FastAPI 进程承载执行线程。
- 沙箱池/容器池复用基础镜像，但每个任务有独立文件系统和资源配额。
- 轨迹、事件、记忆写入异步化；向量检索使用 ANN；模型调用做限流、批量和缓存。
- 先定义 SLO：排队、模型、执行、评审各阶段延迟和失败率，再按瓶颈优化。

### Q64：如果要支持 Java/C++ 题解，如何扩展？

**答题要点：**

- 将语言、编译器、运行命令和测试适配器抽象为 Environment/Runner 接口，不把语言判断散落在 Agent 中。
- 测试契约仍可保持 JSON，但 runner 需要编译、退出码、超时和 sanitizer 结果。
- 工具权限按语言沙箱配置；编译缓存和依赖镜像独立管理。
- Reviewer Prompt 增加语言和编译错误信息，记忆标签加入语言/运行时。

### Q65：如果模型生成了非常慢但样例能过的算法，系统怎么办？

**答题要点：**

- 当前 Reviewer 会提示复杂度，但不会自动做压力测试或拒绝提交。
- 增加约束驱动的压力用例、性能预算、pytest marker、超时和输入规模基准。
- 让 Reviewer 输出结构化复杂度，代码规则或参考实现做二次判断；性能失败也形成 Recovery episode。
- 线上按题型维护隐藏评测集，不能只依赖用户提供样例。

### Q66：如果记忆检索结果和当前题目冲突，谁优先？

**答题要点：**

- 当前题目约束和权威测试优先，记忆只作为 advisory。
- Prompt 中明确经验不是事实答案；模型必须重新分析并执行测试。
- 相关性 Grader 和冲突检测可以丢弃矛盾记忆；轨迹记录注入内容以便复盘。
- 发现错误记忆后应隔离、降权或删除，并重新运行受影响题集。

### Q67：如何支持用户中途取消任务？

**答题要点：**

- 为 RunState 增加 cancel flag/状态机，API 提供 `POST /api/runs/{id}/cancel`。
- Environment 需要终止当前进程树，Worker 释放容器和临时目录，SSE 发送 `run_cancelled`。
- 取消必须幂等，不能让取消后的模型结果再次覆盖终态。

### Q68：如何做版本化和可复现运行？

**答题要点：**

- 轨迹记录 Agent 版本、模型名/参数、Prompt 版本、测试用例 hash、环境镜像和配置 hash。
- 固定随机种子、依赖锁文件、模型温度和 timeout；保存原始模型响应（脱敏后）。
- 记忆检索记录 query、候选、最终选中项和 embedding model，便于解释结果变化。

## 11. 项目经历与行为面试题

### Q69：这个项目最难的部分是什么？

**推荐答法：**

不要只说“调用大模型”。可以说最难的是建立可信闭环：模型具有不确定性，但测试文件不能被修改、提交不能绕过验证、失败经验不能凭空入库。因此把完成条件、文件快照、Recovery episode、Reviewer gate 和事件轨迹都做成了代码级约束，并用 FakeModel 覆盖关键状态转移。

### Q70：你做过的一个关键取舍是什么？

**可选答法：**

- 选择单 Bash 工具：表达力强、模型容易使用，但安全面大，所以加受保护文件、timeout 和后续沙箱计划。
- 选择 SQLite 存记忆：本地零运维、便于验证，但搜索是线性扫描，规模化要迁移 ANN 数据库。
- 选择独立 Reviewer：增加一次模型调用和成本，但能把“本地样例通过”和“算法质量评审”分开。
- 选择固定模型：演示和回归稳定，但供应商绑定；通用化时要做模型适配和能力探测。

### Q71：遇到过什么失败？你如何定位和修复？

**回答结构：**

1. 说明可复现现象：例如提交早于测试、测试文件被改、失败后没有上下文或模型超时。
2. 用 trajectory 的 command/observation 和快照定位状态转移。
3. 增加最小回归测试，先证明 bug，再修改实现。
4. 说明修复后的不变量和残余风险。

不要把没有真实发生过的故事包装成项目经历。

### Q72：你如何证明自己真正理解代码，而不是只会使用模型？

**答题要点：**

- 能从 `POST /api/runs` 一直讲到 `DefaultAgent.execute_actions()`、`LocalEnvironment.execute()`、pytest 判定和 Reviewer。
- 能解释一个非法 tool call 如何变成 FormatError 消息，一个 protected file 修改如何变成 returncode=-1。
- 能现场指出模型错误恢复为何只在 `verified=True` 时成立，以及 SQLite 线性搜索的扩展瓶颈。
- 能运行单测并修改 FakeModel 响应，复现提交门禁和 Recovery 记忆行为。

### Q73：如果重新做一次，你会优先改什么？

**推荐优先级：**

1. 真正的容器/微虚拟机沙箱和进程树资源限制。
2. Worker + 持久化队列，替代单进程后台线程。
3. 参考实现/隐藏压力测试，提升测试生成可信度。
4. 结构化 Reviewer 输出和自动复杂度/性能门禁。
5. Memory 的离线标注评估、ANN 索引、版本迁移和错误经验治理。

## 12. 面试前动手验证清单

- [ ] 能在 1 分钟内说清“题目 -> 测试文件 -> Agent -> Bash -> pytest -> Reviewer -> Memory”。
- [ ] 能指出 `DefaultAgent` 的 `verified`、`step_limit`、提交 sentinel 和模型错误恢复逻辑。
- [ ] 能解释 `test_cases.json` 为什么是权威文件，以及如何阻止篡改。
- [ ] 能现场讲清 Git Bash、cwd、timeout、stdout/stderr 和 protected file 恢复。
- [ ] 能解释 tool call id、JSON 参数解析和 observation 回传。
- [ ] 能按任务检索、Recovery 检索、学习、去重四条线讲 Memory。
- [ ] 能说出 SQLite 线性向量搜索的瓶颈和迁移方案。
- [ ] 能解释 FastAPI 后台线程、RunState 锁、SSE sequence 和 Last-Event-ID。
- [ ] 能明确说出本地通过不等于隐藏测试通过，当前环境不是真正沙箱。
- [ ] 能运行 `python -m pytest -q`，并至少看懂 [`tests/test_agent.py`](tests/test_agent.py)、[`tests/test_memory.py`](tests/test_memory.py)、[`tests/test_api.py`](tests/test_api.py)。
- [ ] 准备一组自己真实测量的数字：成功率、平均步骤、P95、成本、Memory Recall@K；没有就不要编造。

## 13. 可以反问面试官的问题

1. 这个岗位更关注 Agent 编排、模型应用算法，还是执行沙箱和平台工程？
2. 团队如何定义 Agent 任务的离线评测集和线上成功指标？
3. 生产环境更倾向容器沙箱、微虚拟机，还是受限工具 API？
4. 记忆/RAG 系统目前最大的瓶颈是召回质量、上下文成本还是数据治理？
5. 模型供应商切换、Prompt 版本管理和回归评测由哪个模块负责？

## 14. 最后背这一段

本项目的核心不是“让模型写出一段代码”，而是把不可靠的模型输出放进一个可验证、可恢复、可追踪的工程闭环：测试用例和测试脚本由系统掌控，Bash 执行结果真实回传，代码级门禁决定是否完成，独立 Reviewer 负责第二视角，验证过的失败修复再沉淀为可检索经验。面试时要同时讲清算法 Agent 的动态决策、软件工程的不变量和生产系统的安全边界。
