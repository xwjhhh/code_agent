# 算法题编程智能体：需求文档

## 1. 项目定位

本项目面向 LeetCode 等算法编程题，个人独立设计并实现一个轻量级 coding agent。
用户使用自然语言输入题目后，系统调用大语言模型进行分析、编程和自检，并通过本地工具完成文件读写和测试执行，最终输出可运行的题解代码以及代码评审结果。

本项目的重点不是一次性生成代码，而是实现一个能够观察执行结果、根据反馈继续修改代码的 Agent 闭环。

## 2. 目标用户流程

1. 用户在命令行输入算法题目、语言和可选的约束条件。
2. Agent 将任务和当前对话历史发送给大语言模型。
3. 模型通过工具调用查看工作目录、创建或修改源代码和测试文件。
4. Agent 在本地执行测试命令，并把标准输出、错误输出和返回码反馈给模型。
5. 如果测试失败，模型分析错误并继续修改代码、重新测试。
6. 测试通过或达到终止条件后，评审模块检查复杂度、边界情况、可读性和测试覆盖。
7. 系统展示并保存最终代码、测试结果、评审意见和运行轨迹。

核心流程如下：

```text
自然语言题目
    -> 模型规划
    -> 工具调用
    -> 本地执行
    -> 结果反馈
    -> 迭代修复
    -> 测试通过
    -> 大模型评审
    -> 最终输出
```

## 3. 功能需求

### 3.1 输入

- 支持命令行输入题目文本，或从文本文件读取题目。
- 支持指定编程语言，第一版至少支持 Python。
- 支持指定模型名称、API 地址和最大迭代次数。
- API Key 只能从环境变量或未入库的本地配置文件读取。

### 3.2 工具

工具由本项目自行定义、解析和执行，第一版至少包含：

- `read_file(path)`：读取工作目录内的文件。
- `write_file(path, content)`：创建或覆盖工作目录内的文件。
- `run_command(command)`：在工作目录内执行测试或其他命令，返回输出和返回码。

工具执行必须限制在项目工作目录内，并设置命令超时，避免越权访问和进程长期占用。

### 3.3 Agent 主循环

- 初始化 system prompt、任务消息和工具说明。
- 保存每一轮的用户消息、模型消息、工具调用和工具结果。
- 解析模型返回的工具调用及 JSON 参数。
- 按顺序执行工具并把结果追加回对话历史。
- 模型没有工具调用且明确完成任务时结束。
- 支持最大轮数、总耗时和可选费用上限。
- 对非法工具名、参数错误、命令超时、模型请求失败和空响应进行处理，并给模型返回可继续修复的错误信息。

### 3.4 代码生成与验证

- Agent 应把题解写入约定的源代码文件。
- Agent 应生成至少一组样例测试，并优先使用标准测试命令运行。
- 测试失败时，至少允许模型再次修改并重试。
- 最终结果应包含源代码、测试输出、退出状态和迭代次数。

### 3.5 代码评审

测试完成后调用一次评审模型，评审内容至少包括：

- 算法正确性和主要边界情况；
- 时间复杂度和空间复杂度；
- 可读性和实现中的明显风险；
- 测试是否覆盖关键场景。

评审结果只作为分析和展示，不替代本地测试。

### 3.6 运行记录

- 保存完整对话和工具执行轨迹，便于调试和演示。
- 记录每次调用的时间、工具名、参数摘要、返回码和输出。
- 输出中不得包含 API Key 或其他敏感凭据。

## 4. 设计边界与题目合规要求

- 允许使用 LiteLLM、OpenAI 兼容客户端或模型原生 tool calling 作为模型调用层。
- LiteLLM 只负责模型 API 适配、请求发送和响应接收，不负责 Agent 循环或本地执行。
- 对话历史、工具定义、工具调用解析、文件读写、命令执行、反馈循环、终止条件和错误处理必须由本项目自行实现。
- 不使用 LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等 Agent 框架或 SDK。
- 不使用 Code Interpreter、Files API 等 API 服务端托管的执行或文件工具。
- 不在现成 Agent 产品上封装界面；项目应有独立的主循环和本地工具执行代码。

## 5. 推荐模块划分

```text
src/
  main.py              命令行入口和任务配置
  agent.py             Agent 状态、上下文和主循环
  model_client.py      LiteLLM 或 OpenAI 兼容接口封装
  tools.py             工具 schema、参数校验和分发
  executor.py          文件操作、命令执行、超时和工作目录限制
  reviewer.py          最终代码评审
tests/                 工具、循环和端到端测试
workspace/             Agent 生成代码和测试的运行目录
```

上述划分是实现建议，不要求机械照搬；重要的是各部分职责清晰且核心逻辑可在面试中解释。

## 6. 验收标准

给定一道包含样例和约束的算法题，系统应能够：

1. 接收自然语言任务并启动 Agent。
2. 至少调用一次本地工具，将代码写入文件。
3. 创建或使用测试，并在本地执行测试命令。
4. 测试失败时读取错误并再次修改代码。
5. 在测试通过、模型主动完成或达到限制时稳定结束。
6. 输出最终代码、测试结果和评审意见。
7. 在无效工具调用、命令失败或模型异常时不崩溃，并保存可诊断的轨迹。

## 7. 演示建议

视频控制在两分钟内，演示一道有代表性的算法题。最好展示一次测试失败后的自动修复过程，随后展示测试通过、复杂度评审和最终文件内容。视频中不要展示 API Key。

## 8. 后续可选增强

- 增加 Java、C++ 等语言模板和编译测试。
- 增加 Docker 沙箱执行。
- 增加 diff 展示、重试策略和上下文压缩。
- 增加多测试用例生成和独立评测模型。
- 增加 Web 界面，但不改变核心 Agent 和本地工具逻辑。
